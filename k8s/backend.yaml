apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 0
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: sujeetsr07/backend:latest
        ports:
        - containerPort: 8080
